# MongoDB 连接配置
# 方式1：使用 Docker Compose 启动真实 MongoDB（推荐）
#   执行：docker-compose up -d
MONGODB_URL=mongodb://admin:admin123@localhost:27017/opinion_monitor?authSource=admin

# 方式2：如果没有 MongoDB，系统会自动降级到内存模拟（仅用于开发测试，数据不持久化）

# 数据库名
MONGO_DB_NAME=opinion_monitor

# 服务端口
PORT=8001

# 环境
ENV=development
